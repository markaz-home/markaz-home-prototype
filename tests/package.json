{
  "name": "@markaz/tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:integration": "vitest run integration",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@markaz/db": "workspace:*",
    "@markaz/domain": "workspace:*",
    "drizzle-orm": "^0.38.3",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@markaz/config": "workspace:*",
    "dotenv": "^16.4.7",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
