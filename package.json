{
  "name": "markaz-home",
  "private": true,
  "version": "0.1.0",
  "description": "MARKAZ Home — UAE (Dubai-first) property marketplace prototype: auth/onboarding, listings, publication, marketplace, offers, simulated transactions, and a separate Admin operations portal (Weeks 1–8).",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:e2e": "pnpm --filter @markaz/web test:e2e && pnpm --filter @markaz/admin test:e2e",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,mjs,json,md,css,yml,yaml}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,mjs,json,md,css,yml,yaml}\"",
    "config:check": "node --env-file-if-exists=.env --env-file-if-exists=.env.local packages/config/environment-cli.mjs all",
    "security:secrets": "node scripts/scan-secrets.mjs",
    "security:audit": "pnpm audit --prod --audit-level high",
    "db:generate": "pnpm --filter @markaz/db generate",
    "db:migrate": "supabase migration up",
    "db:seed": "pnpm --filter @markaz/db seed",
    "supabase:start": "supabase start",
    "supabase:stop": "supabase stop",
    "supabase:reset": "supabase db reset",
    "supabase:status": "supabase status",
    "db:setup": "pnpm --filter @markaz/db run setup:demo",
    "db:setup-uae-pass": "pnpm --filter @markaz/db run setup:uae-pass"
  },
  "devDependencies": {
    "@markaz/config": "workspace:*",
    "prettier": "^3.4.2",
    "prettier-plugin-tailwindcss": "^0.6.9",
    "supabase": "^2.6.8",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
